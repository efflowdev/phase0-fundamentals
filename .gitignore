__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
.env
node_modules/

# SQLite sidecars. runs.db itself is deliberately not ignored — it is the
# experiment data, and lens reads it on Monday.
*.db-wal
*.db-shm

# Derived: rebuilt from the committed cache in ~12s by evaluate.py
day4_search/index/
