__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.venv/
.env
node_modules/

# SQLite sidecars. runs.db itself is deliberately not ignored — it is the
# experiment data, and lens reads it on Monday.
*.db-wal
*.db-shm
